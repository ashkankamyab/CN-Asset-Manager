apiVersion: v1
kind: Service
metadata:
  name: {{ include "cn-asset-manager.fullname" . }}
  labels:
    {{- include "cn-asset-manager.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "cn-asset-manager.selectorLabels" . | nindent 4 }}
